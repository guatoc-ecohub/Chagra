# deepseek · INFORME 087 R3 — `Check bundle sizes` ya no bloquea la base (fix mergeado; bisección completada)

Fecha: 2026-09-05 · Carril: deepseek (opencode) · Base verificada: `origin/dev` @ `1e454471e`

## Veredicto en una línea

**La base está VERDE para `Check bundle sizes` y ningún PR abierto está bloqueado por el gate.**
El fix ya está mergeado en `origin/dev` (PR #3099 + c54a53a42). La hipótesis del brief sobre el commit
culpable (`588c878d0`) se CONFIRMA a nivel chunk con medición antes/después. No hace falta PR nuevo:
sería ruido duplicando #3099.

## Estado actual de la base (medido, no inferido)

1. **CI real sobre el tip de dev** (workflow `Performance Budget`, job `Check bundle sizes`),
   SHA `1e454471e` = tip de `origin/dev` hoy 00:52 -05:
   - run `33948338950` (2026-09-05T05:52Z): **SUCCESS**
   - salida: `Total dist (arranque, budget): 40.0 MB / 42.0 MB` · `Chunk count: 568` ·
     `Grafo de arranque verificado: 36 chunks estáticos (ninguna familia lazy presente)` · exit 0
2. **Reproducción local independiente** (build limpio de `origin/dev` en worktree propio, `npm run build`
   exit 0 + `node scripts/check-perf-budget.mjs`):
   ```
   Total dist (arranque, budget): 40.0 MB / 42.0 MB
   Excluido lazy (modo campo #2088): 127.3 MB (cache-on-use, no en arranque)
   Main bundle: 0 B
   Chunk count: 568
   Grafo de arranque verificado: 36 chunks estáticos (ninguna familia lazy presente)
   All budgets within thresholds.   EXIT=0
   ```
   Idéntico a CI. Log: `_gate/087/build-dev-r3.log`.
3. **Los 8 PRs abiertos con base dev** (#2859, #3124, #3147, #3148, #3149, #3150, #3151, #3152):
   los últimos 10 runs del workflow son todos SUCCESS; ninguno con `Check bundle sizes` rojo.
4. **#3097 y #3098 (los PRs "inocentes" del brief): MERGED, con `Check bundle sizes` SUCCESS**
   (runs del 2026-09-04T02:00Z y 02:10Z, después del fix). El bridge de case-bridge no causó el rojo.

## Bisección del commit culpable (criterio de aceptación 1)

Hipótesis del brief: `588c878d0 feat(compai): trazar chivito y luciernaga con receta jaguar`
(2026-08-31 03:27). Verificado construyendo el padre directo y el propio commit:

| commit | fecha | medición (build limpio, tooling de su era) | veredicto |
|---|---|---|---|
| `ff08db3fc` (padre directo, = `588c878d0^`) | 2026-08-27 | `TOTAL 25.0 MB / 27.5 MB`, sin chunks de tinta en `dist/assets/` | **verde** |
| `588c878d0` (sospechoso) | 2026-08-31 | `CHUNK "creatures-Ca-sD0wA.js" exceeds 500KB: 1.4 MB` · `TOTAL 26.3 MB / 27.5 MB` | **rojo** |

- El rango `ff08db3fc..588c878d0` tiene **UN solo commit** (`git rev-list --count` = 1): la atribución
  es limpia, sin commits intermedios que confundan.
- `588c878d0` es el **único commit** que introduce `src/visual/creatures/trazadoPayloads.js`
  (`git log --all --follow`), el módulo que rolldown emite como el chunk de 1.4 MB.
- En el build del propio `588c878d0` el chunk infractor se llamaba `creatures-*` (aún no existía la
  exclusión `creatures-` en el script de esa era). El nombre `trazadoCreature-*` y el rojo de TOTAL
  43.0 MB que cita el brief corresponden al estado posterior (base `96202ac85`, run CI 33741072947)
  tras los refactors de consolidación del elenco (#3040/#3064): el payload compartido se re-emitió con
  ese nombre y la allowlist literal (`^JaguarTrazado-`, `creatures-`) no lo cubría.
- **Conclusión**: la hipótesis se confirma. `588c878d0` cruzó el umbral por-chunk de 500 KB con el arte
  de tinta; el rojo que persistía en la base era ese mismo arte **EAGER disfrazado de lazy**
  (raíz documentada en INFORME-087 R2: imports estáticos + falta de `sideEffects`), no los PRs
  de case-bridge.

## Por qué está verde hoy y no se apagó el gate

El fix mergeado en dev (c54a53a42 el 2026-08-31 como paliativo, y **c82bbfc21 / PR #3099** el
2026-09-03 como fix real) hace tres cosas, todas verificadas en el script actual de `origin/dev`
(`scripts/check-perf-budget.mjs`, idéntico al que corrió CI):

1. El arte de tinta pasó a **lazy de verdad** (code-split en `src/App.jsx` +
   `ChagraAgentAvatarAdaptadoresSync.js`); el grafo eager de arranque quedó sin ninguna familia lazy
   (CI: 36 chunks estáticos, cero trazado/creatures/vendor-three).
2. La allowlist por chunk dejó de ser literal (`^JaguarTrazado-`) y pasó a familia
   (`/^[a-z]*trazado/i`), que cubre `trazadoCreature-*` y futuros `*Trazado-*` sin reeditar la lista.
3. **Verificación lazy-vs-grafo fail-closed**: el gate recalcula el cierre estático real del dist y
   falla con `LAZY MENTIRA` si un chunk exceptuado como lazy aparece en el arranque — el modo de fallo
   exacto de la base queda cazado por construcción (control negativo en INFORME-087 R2).

Sin sacrificio de arte: no se degradó ningún rig a PNG; la exclusión es lazy de verdad, no recorte.

## Frente TOTAL 43 MB (contexto, ya resuelto)

El arreglo del chunk bajó TOTAL 43.0 → 40.0 MB (verde, umbral 42 MB) porque el arte pasó a lazy real y
salió del techo de ARRANQUE (misma política que vendor-three/valle/tfjs). El umbral del TOTAL además
cambió de era (27.5 → 42 MB) entre el padre y hoy; por eso 25.0/26.3 MB (era 27.5) y 40.0/43.0 MB
(era 42) no son directamente comparables entre sí — se documentan con su era para no confundir.

## Qué NO verifiqué

- **Runtime en navegador** (red/timing de descargas del arte al montar avatar): la evidencia de
  laziness es el grafo estático del dist emitido + el gate fail-closed, no una sesión de browser.
- **Suite vitest / eslint completos** en esta corrida: no los ejecuté (deuda conocida aparte; ver
  INFORME-087 R2: 2 fallos preexistentes de `CompaiP1.contract.test.jsx`, lint OOM preexistente).
- **`tsc:check vs baseline`**: frente aparte, no lo toqué (instrucción del brief).
- **Bisección del TOTAL 43 MB commit-a-commit**: medí el cruce por-chunk (el gate que bloqueaba);
  el crecimiento del TOTAL entre era 27.5 y era 42 MB es acumulativo de muchos commits y lo dejé
  documentado en R2 (composición del dist) como frente aparte — no lo desglosé commit a commit.
- **Por qué no abro PR**: el fix del encargo ya está en `origin/dev` (PR #3099 mergeado) y la base
  pasa el gate con el criterio de aceptación 3 satisfecho (CI sobre dev y sobre PRs). Un PR nuevo con
  el mismo fix sería ruido. Si el operador quiere el cierre formal del item 087 en la cola, este
  informe es la constancia de aceptación.
