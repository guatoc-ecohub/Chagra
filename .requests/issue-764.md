# Request #764

- Issue: https://github.com/guatoc-ecohub/Chagra/issues/764
- Title: [add][catalog] eugenia_stipitata (Arazá) — nueva species colombiana
- Author: @guatoc-ecohub
- Status: awaiting code-generation

## Body

Tipo: chore Scope: catalog Descripcion:

**AGREGAR NUEVA species al catálogo** (Nemotron). NO refinar existente — esta species NO está en el catálogo aún.

**SPECIES NUEVA:** `eugenia_stipitata` — Arazá (`Eugenia stipitata McVaugh`)
- **Categoría:** frutales_nativos
- **Thermal zones (anchor):** calido
- **Notas operador:** Amazonía colombiana

**ACCIÓN:**
Agregar nuevo objeto species al array `species[]` en `catalog/chagra-catalog-seed-v3.1.json`. Usa el schema completo conforme a entries existentes (ej. busca `oxalis_tuberosa` o `solanum_phureja` como template). REQUERIDO:

1. `id`, `nombre_comun`, `nombre_cientifico`, `familia_botanica`, `category` (ya provistos).
2. `thermal_zones`: array de zonas dadas arriba.
3. `roles_in_guild`: array (típico: `["crop"]`, `["forestal"]`, `["medicinal"]`).
4. `cultivable: true` (a menos que sea forestal silvestre).
5. `conservation_status`: usar `"cultivo_comun"` para cultivos, `"naturalizada"` para introducidas, `"endemica_colombia"` o `"IUCN_LC/VU/EN"` si aplica.
6. `altitud_msnm`: `{min_absoluto, optimo_min, optimo_max, max_absoluto}` (4 valores integers en msnm).
7. `temperatura_c`: `{helada_letal, optimo_min, optimo_max, max_tolerable}` (4 valores en °C).
8. `radiacion`: `"sol_pleno"` | `"sombra_parcial"` | `"sombra"`.
9. `agua`: `"bajo"` | `"medio"` | `"alto"`.
10. `drenaje_requerido`: `"excelente"` | `"bueno"` | `"medio"`.
11. `propagation`: `{metodo_principal, notas}`.
12. `source_ids`: ≥4 fuentes Tier A canónicas:
    - `gbif-taxonomic-backbone`, `powo-kew`
    - `agrosavia-sol-andina` o `agrosavia-manual-*` según aplique
    - `ica-resolucion-3168-2015` o `nrc-1989-cultivos-andinos` según relevancia
13. `valor_pedagogico`: ≥ 200 chars con 4 elementos:
    a) Distribución Colombia (departamentos + altitud + thermal_zone)
    b) Manejo agronómico (propagación + ciclo + asociaciones + plagas)
    c) Contexto cultural muisca/andino/campesino si aplica
    d) Citación fuente Tier A explícita
14. `validation_level: "ai_draft"` (para que operador haga review posterior).

**RESTRICCIONES CRÍTICAS:**
- **SOLO AGREGAR** este nuevo species (`eugenia_stipitata`). NO modificar species existentes.
- **NO romper la sintaxis JSON.** El array `species[]` debe quedar válido (cada objeto separado por coma, último sin coma).
- Indentación: el nuevo objeto usa 4 espacios para `{`, 6 espacios para fields (mismo patrón que vecinos).
- **NO inventar datos sin fuente.** Si no encuentras altitud/temperatura específica, usa rangos conservadores documentados en GBIF/POWO para la especie.

CRITERIO ACEPTACIÓN:
- [ ] JSON parseable (`python -m json.tool catalog/*.json > /dev/null`).
- [ ] Solo 1 species nueva agregada (no modificar otras).
- [ ] vp ≥ 200 chars con 4 elementos.
- [ ] ≥4 source_ids Tier A.

🤖 Generated by 450-autopilot. Generated with [Claude Code](https://claude.com/claude-code)

---

**Esto es un stub.** El agente de code-generation reemplazará este archivo y/o hará los cambios solicitados cuando sea invocado con la label `ready-to-generate` (flujo futuro).
