/** Rutas canónicas expuestas por la portada campesino-first. */
export const CAMPESINO_HOME_ROUTES = Object.freeze({
  mercado: 'mercados',
  voz: 'registro_unificado',
});

export default CAMPESINO_HOME_ROUTES;
