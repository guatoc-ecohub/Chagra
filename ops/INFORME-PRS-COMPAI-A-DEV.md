# Informe de PRs Compai hacia `dev`

Fecha de verificación: 2026-08-23. Alcance: PRs con `baseRefName=dev` y `mergeable=MERGEABLE`. No se forzaron merges ni se tocaron PRs con otra base.

2996 | no-mergeado | `base=dev`, `mergeable=MERGEABLE`, `mergeStateStatus=UNSTABLE`; checks fallidos: `CLAAssistant fail`, `Check bundle sizes fail` (x2), `tsc:check vs baseline fail` (x2).
2995 | no-mergeado | `base=dev`, `mergeable=MERGEABLE`, `mergeStateStatus=UNSTABLE`; checks fallidos: `CLAAssistant fail`, `Check bundle sizes fail` (x2), `tsc:check vs baseline fail` (x2), `E2E suite completa (informativo) fail`.
2992 | no-mergeado | `base=dev`, `mergeable=MERGEABLE`, `mergeStateStatus=UNSTABLE`; checks fallidos: `CLAAssistant fail`, `Check bundle sizes fail`, `tsc:check vs baseline fail`, `E2E suite completa (informativo) fail`.
2990 | no-mergeado | `base=dev`, `mergeable=MERGEABLE`, `mergeStateStatus=UNSTABLE`; checks fallidos: `CLAAssistant fail`, `Check bundle sizes fail` (x2), `tsc:check vs baseline fail` (x2), `E2E suite completa (informativo) fail`.
2987 | no-mergeado | `base=dev`, `mergeable=MERGEABLE`, `mergeStateStatus=UNSTABLE`; checks fallidos: `CLAAssistant fail`, `Check bundle sizes fail` (x2), `tsc:check vs baseline fail` (x2), `E2E suite completa (informativo) fail`.
2985 | no-mergeado | `base=dev`, `mergeable=MERGEABLE`, `mergeStateStatus=UNSTABLE`; checks fallidos: `CLAAssistant fail`, `Check bundle sizes fail` (x2), `tsc:check vs baseline fail` (x2), `E2E suite completa (informativo) fail`.
2984 | no-mergeado | `base=dev`, `mergeable=MERGEABLE`, `mergeStateStatus=UNSTABLE`; checks fallidos: `CLAAssistant fail`, `Check bundle sizes fail` (x2), `tsc:check vs baseline fail` (x2), `E2E suite completa (informativo) fail`.
2975 | no-mergeado | `base=dev`, `mergeable=MERGEABLE`, `mergeStateStatus=UNSTABLE`; checks fallidos: `CLAAssistant fail`, `Check bundle sizes fail` (x2), `E2E suite completa (informativo) fail`.
2962 | no-mergeado | `base=dev`, `mergeable=MERGEABLE`, `mergeStateStatus=UNSTABLE`; checks fallidos: `Check bundle sizes fail`, `tsc:check vs baseline fail`.
