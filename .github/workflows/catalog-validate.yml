name: Catalog Validate

# Corre validate-catalog.mjs cuando un PR toca catalog/ o el schema. Es un
# merge-gate adicional barato (segundos, sin npm install ni build), separado
# de los gates pesados (CodeQL + Playwright) para feedback rápido a species
# refiners.
#
# Modo CI: --lenient-schema (sin --seed-mode). Schema errors legacy quedan como
# warnings (cleanup pendiente fuera de este workflow). AMB-10/11/13/18 y demás
# checks duros SÍ fallan — ahí es donde catchea bugs nuevos. El --seed-mode
# fue removido el 2026-05-20 (regresión AMB-10 0→234 entre 2026-05-17/18 lo
# expuso: degradaba asimetrías de companions de error a warning).

on:
  pull_request:
    paths:
      - 'catalog/**/*.json'
      - 'scripts/validate-catalog.mjs'
      - 'scripts/audit-contaminacion.mjs'
      - 'scripts/audit-contaminacion-baseline.json'
      - '.github/workflows/catalog-validate.yml'
  # También en push a main para detectar regresiones (si alguien hace push
  # directo bypassing PRs — branch protection lo bloquea pero defense in depth).
  push:
    branches: [main]
    paths:
      - 'catalog/**/*.json'

permissions:
  contents: read

concurrency:
  group: catalog-validate-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node 22
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Run catalog validator (lenient, strict semantic)
        run: node scripts/validate-catalog.mjs --lenient-schema

      # Gate anti-regresión de contaminación de datos (queue: test de
      # contaminación catálogo+grafo). Solo Node built-ins, sin npm install.
      # Falla si aparece un hallazgo NUEVO (fingerprint no presente en
      # scripts/audit-contaminacion-baseline.json) — bajar el baseline
      # (arreglar datos) siempre está permitido vía --update-baseline.
      - name: Run data contamination gate
        run: node scripts/audit-contaminacion.mjs --check

      # Comentario informativo en el PR cuando hay warnings significativos.
      # Si AMB-16 sigue creciendo (vp <200 chars), señala que el batch de
      # refinement no está cerrando el backlog.
      - name: Summarize warnings
        if: always()
        run: |
          echo "## Validator summary" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          node scripts/validate-catalog.mjs --lenient-schema 2>&1 | tail -30 >> $GITHUB_STEP_SUMMARY || true
          echo '```' >> $GITHUB_STEP_SUMMARY
